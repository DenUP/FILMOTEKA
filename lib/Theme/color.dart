import 'package:flutter/material.dart';

abstract class colors {
  static const purple2 = Color.fromRGBO(82, 82, 199, 1);
  static const error = Color.fromRGBO(233, 80, 80, 1);
  static const tertiary = Color.fromRGBO(189, 189, 189, 1);
}
